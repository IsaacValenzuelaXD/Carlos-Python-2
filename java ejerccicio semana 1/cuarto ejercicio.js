// Variables de entrada (tipo Number con decimales)
let a = 10.0;
let b = 1.0;
let c = 3.0;

// Expresión aritmética con todas las operaciones requeridas
let resultado = (a ** 2) + (b / 4.0) - (c * 0.0);

// Mostrar resultado
console.log("Resultado:", resultado);
