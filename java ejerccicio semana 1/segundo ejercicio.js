// Solicitar datos al usuario
let nombre = prompt("Ingrese su nombre:");
let cargo = prompt("Ingrese su cargo (ej. Analista):");

// Mostrar mensaje personalizado
console.log(`Acceso concedido: Bienvenido, ${nombre}, al sistema de ${cargo}.`);
